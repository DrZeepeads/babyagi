# Autonomous Agent Dashboard (Frontend)

## Development

```bash
cd autonomous-agent-dashboard
npm install # or pnpm install
npm run dev
```

The app will be available at `http://localhost:5173`.

## Environment Variables

Copy `.env.example` to `.env` and set `VITE_API_URL` and `VITE_API_KEY`.